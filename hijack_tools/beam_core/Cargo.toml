[package]
name = "beam_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "beam_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module"] }
numpy = "0.29"
